# Public source synchronization

Source revision: `728161150cbd8cc5279712d60e5b7c5bbb6f3e50`.

This revision synchronizes per-model script, voice, calibrated and selected duration hints in both video workspace layouts; read-only recovery of the initial script reference; original-media version selection and processing labels; and the 12-hour unpaid recharge-order expiry policy, including verified late-payment settlement.

Source version changes preserve edit timing and do not delete original files. Existing saved subtitle styles remain unchanged; the larger default subtitle size is retained from the preceding public release.

Only the online public edition is included. Local processing agents, GPU runners, node workflows, production configuration and operational release artifacts remain excluded. Processing labels describe persisted media provenance and do not enable a local processing service in this edition.

Regression coverage includes source selection rollback, shorter-source rejection, per-model timing, script-history ordering, recharge expiry and idempotent late settlement. Public import, authorization, frontend, source-boundary and manifest checks must pass before publication.
